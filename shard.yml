name: gh
version: 0.2.1

authors:
  - mosop

crystal: 0.20.5

license: MIT

dependencies:
  global_config:
    github: mosop/global_config
    version: ~> 0.1.0

development_dependencies:
  crystal_plus:
    github: mosop/crystal_plus
    version: ~> 0.1.2
